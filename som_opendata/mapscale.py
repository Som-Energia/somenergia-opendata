class MapScale(object):

    def __init__(self, lower, higher, type='linear'):
        self.low = lower
        self.high = higher
        self.type = type

    def __call__(self, point):
        val = 
        return round(val, 2)
